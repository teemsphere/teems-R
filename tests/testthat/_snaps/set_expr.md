# set intersection aborts on disagreeing origin coverage

    x Set operator `&` in the definition of C: element x is carried with different origin coverage by the two operands.
    i The operands aggregate different source elements into this shared element, so the intersection is ambiguous; align the aggregation mappings that build both sets.

