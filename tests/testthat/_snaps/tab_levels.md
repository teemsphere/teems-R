# malformed Formula & Equation aborts

    x Malformed `Formula & Equation` statement: expected `Formula [(initial)] & Equation [(levels)] name [quantifiers] lhs = rhs` (GEMPACK manual 10.9.1): "Formula & Equation E_BAD (all,r,REG) qgdp(r)"

# c_-leading levels variable name aborts

    x Levels variable "c_bad" starts with `c_`, colliding with the change-reference column prefix; the solver cannot carry such names.
    i Rename the variable.

