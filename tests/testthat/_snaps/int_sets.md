# empty or inverted time ranges abort

    x Intertemporal set BADT has an empty or inverted time range: "P[3] - P[1]" resolves to p[3] - p[1].
    i With 5 time steps the valid indices are `p[0]` through `p[4]`.

# out-of-range time indices abort

    x Intertemporal set BADT has an out-of-range time range: "P[0] - P[NTSP]" resolves to p[0] - p[5].
    i With 5 time steps the valid indices are `p[0]` through `p[4]`.

# malformed intertemporal terms abort

    x Malformed intertemporal set definition for BADT: "P[frog]".

