# unknown dot arguments abort

    x Unknown argument `bogus_argument` passed to `...`.
    i `...` accepts the Runge-Kutta step controls only: `adaptive`, `eps_tolerance`, `max_retries` and `retry_adjust`. See `ems_RK()` for the dedicated Runge-Kutta interface.

