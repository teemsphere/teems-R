% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ems_probe.R
\name{ems_probe}
\alias{ems_probe}
\title{Structural probe of a deployed model}
\usage{
ems_probe(cmf_path, fine = TRUE, ...)
}
\arguments{
\item{cmf_path}{Character length 1, path to the CMF file generated
by \code{\link[=ems_deploy]{ems_deploy()}}.}

\item{fine}{Logical length 1 (default \code{TRUE}). Also run the fine
Dulmage-Mendelsohn decomposition: the strongly-connected-component
view of the system — its irreducible simultaneous cores versus the
recursively solvable remainder — including the composition of the
largest cores by equation and variable.}

\item{...}{Additional named solver arguments: the MA48 workspace
initial guesses (\code{laA}, \code{laD}, \code{laDi}) and the expert solver
flags (\code{fastrefac}, \code{gpzerodivide}, \code{cntl_3}, \code{cntl_6},
\code{nsbbdblocks}, \code{withmc66}, \code{smllthreads}, \code{tempdir},
\code{nowrites}); see the \code{\link[=ems_solve]{ems_solve()}} \code{...} documentation.
Anything else is an error, never a silently ignored flag.}
}
\value{
A \code{teems_probe} object: validity verdict and rank per
pattern, named defect tibble, statement-level incidence tibbles
(\code{statements}, \code{incidence}), core structure (\code{cores}), ordering
evidence (\code{structure}), and report paths.
}
\description{
Runs the solver's structural probe (\verb{-solmed probe}) on
a deployed model: the full pre-solve pipeline (data, formulas,
closure, ordering) followed by an HSL_MC79 maximum-matching /
Dulmage-Mendelsohn diagnosis of the condensed Jacobian — without
solving. The probe validates the closure structurally, catches the
zero-flow singularity class (structurally present but zero-valued
at base data), names any defective variable and equation elements,
and returns the statement-level equation-system structure.
}
\details{
The probe runs on a single MPI rank; its cost is the
pre-solve pipeline plus the matching (milliseconds at 10^4
equations, ~a minute at 10^6). A structurally singular result does
not error here — the object reports it (see
\code{\link[=plot.teems_probe]{plot.teems_probe()}} and the \code{defects} tibble). Use
\code{ems_solve(pre_probe = TRUE)} to abort a solve on structural
singularity instead.
}
\examples{
\dontrun{
# The following examples require the teems solver to be built.
# See https://teemsphere.github.io/ to get started.

probe <- ems_probe(cmf_path)
probe
plot(probe, type = "incidence")
plot(probe, type = "cores")
}
}
\seealso{
\code{\link[=ems_deploy]{ems_deploy()}} for generating \code{"cmf_path"};
\code{\link[=plot.teems_probe]{plot.teems_probe()}} for the incidence, Dulmage-Mendelsohn and
core visualizations; \code{\link[=ems_solve]{ems_solve()}} and its \code{pre_probe} argument.
}
