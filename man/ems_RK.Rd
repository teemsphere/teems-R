% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ems_RK.R
\name{ems_RK}
\alias{ems_RK}
\title{Solve a model with a Runge-Kutta method}
\usage{
ems_RK(
  cmf_path,
  solution_method = c("DoPri54", "BoSha32", "RK4", "RK2"),
  steps = 4L,
  adaptive = NULL,
  eps_tolerance = 0.1,
  max_retries = NULL,
  retry_adjust = NULL,
  ...
)
}
\arguments{
\item{cmf_path}{Character length 1, path to the CMF file
generated by \code{\link[=ems_deploy]{ems_deploy()}}.}

\item{solution_method}{The Runge-Kutta flavor, one of
\code{"DoPri54"} (default), \code{"BoSha32"}, \code{"RK4"} or \code{"RK2"}. The
embedded pairs (\code{"DoPri54"}, \code{"BoSha32"}) carry the per-step
error estimate that \code{adaptive} control acts on.}

\item{steps}{Integer length 1 (default \code{4L}), the initial number
of steps. Runge-Kutta methods use no Richardson extrapolation,
so no step-count triple is involved; under \code{adaptive} control
this is the initial step count only.}

\item{adaptive}{Character length 1, adaptive step-size control
for the embedded Runge-Kutta methods (\code{"BoSha32"},
\code{"DoPri54"}). Default \code{NULL} resolves to \code{"yes"} for the
embedded pairs and \code{"no"} for \code{"RK2"}/\code{"RK4"} (which provide no
error estimate). Choices:
\itemize{
\item \code{"no"}: Fixed steps.
\item \code{"yes"}: After each step the worst per-component error
metric is compared against \code{eps_tolerance}; failing steps are
redone with a smaller step size and passing steps adjust the
next step size (at most halving or doubling it). A step on
which a percentage-change variable crosses \verb{-100\%} is also
retried at a reduced step size.
\item \code{"accuracy-only"}: As \code{"yes"}, but only the error metric is
acted on; check failures are ignored.
}}

\item{eps_tolerance}{Numeric length 1 (default is \code{0.1}), the
per-step error-metric bound targeted by \code{adaptive} control.
\code{0.1} suffices for most simulations; use \code{0.01} for more
accurate solutions. Values below \code{0.005} are hard to achieve
numerically. Ignored when \code{adaptive = "no"}.}

\item{max_retries}{Integer length 1 (default \code{NULL}, solver
default \code{3L}), \code{adaptive = "yes"} only: how many times a step
failing the -100\% crossing check is retried at reduced length
before the run aborts.}

\item{retry_adjust}{Numeric length 1 in (0, 1) (default \code{NULL},
solver default \code{0.5}), adaptive control only: the step-length
multiplier applied on each retry.}

\item{...}{All other \code{\link[=ems_solve]{ems_solve()}} arguments
(\code{matrix_method}, \code{n_tasks}, \code{precision}, \code{terminal_run}, ...),
forwarded unchanged.}
}
\value{
As \code{\link[=ems_solve]{ems_solve()}}.
}
\description{
A Runge-Kutta front end for \code{\link[=ems_solve]{ems_solve()}}: the same
solver run restricted to the Runge-Kutta \code{solution_method}
flavors, with the step-control arguments that only exist for
them (\code{adaptive}, \code{eps_tolerance}, \code{max_retries},
\code{retry_adjust}) and RK-tuned defaults — \code{"DoPri54"} with a
single step and adaptive control enabled. Everything else
(\code{matrix_method}, \code{n_tasks}, \code{precision}, ...) is forwarded to
\code{\link[=ems_solve]{ems_solve()}} unchanged, which performs all validation and
remains fully capable of Runge-Kutta runs itself.
}
\examples{
\dontrun{
# The following examples require the teems solver to be built.
# See https://teemsphere.github.io/ to get started.

# Adaptive DoPri54 (the defaults):
ems_RK(cmf_path)

# Fixed-step RK4:
ems_RK(cmf_path, solution_method = "RK4", steps = 8L)
}
}
\references{
Schiffmann, F. (2022), "Runge Kutta integrators for
fast and accurate solutions in GEMPACK".
}
\seealso{
\code{\link[=ems_solve]{ems_solve()}} for the general interface and the full
argument documentation.
}
