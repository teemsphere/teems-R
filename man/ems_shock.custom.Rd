% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ems_shock_cstm.R
\name{ems_shock.custom}
\alias{ems_shock.custom}
\title{Specify custom shock}
\usage{
\method{ems_shock}{custom}(var, type = "custom", input, ...)
}
\arguments{
\item{var}{Character of length 1, the variable to be shocked.}

\item{type}{Character of length 1, the type of shock. Choices:
\itemize{
\item \code{"uniform"}: a homogenous shock applied to the specified
variable \code{"var"} or variable elements (using \code{...}) at the
specified \code{"value"}.
\item \code{"custom"}: a user-specified granular shock applied to
variable \code{"var"} and aggregated set-specific variable tuples
according to percentage-change values.
\item \code{"scenario"}: a user-specified granular shock for temporally dynamic
models applied to variable \code{"var"} and unaggregated tuples
according to values specified in \code{"input"}. \code{"scenario"}
shocks must encompass all values associated with tuples
corresponding to unaggregated set elements. Values in
}}

\item{input}{character length 1, path to a csv file; a data
frame or data frame extension (e.g., tibble, data table).
Must contain "Value" as last column representing
percentage-change shocks to each respective tuple.}

\item{...}{Future extension.}
}
\description{
\code{ems_shock.custom()} loads custom shocks for
processing as well as conducts a series of compatibility
checks. A custom shock is one which allows for granular
control over heterogenous shocks that are allocated on a
tuple-specific basis, inputted as a CSV file or dataframe
(or dataframe equivalent) through \code{"input"}.  The accepted
set values within \code{"input"} depend on the \code{"var"} specified
and set mappings associated with this variable. If a custom
shock is to be carried out, the output of this function is a
required input to the \code{"shock"} argument within the
\code{\link[=ems_deploy]{ems_deploy()}} function.
}
\examples{
REGr <- c("asia", "eit", "lam", "maf", "oecd")
ACTSa <- c("svces", "food", "crops", "mnfcs", "livestock")
ALLTIMEt <- c(0, 1, 2, 3, 4, 5)

aoall <- expand.grid(ACTSa = ACTSa,
                     REGr = REGr,
                     ALLTIMEt = ALLTIMEt,
                     stringsAsFactors = FALSE)

aoall <- aoall[do.call(order, aoall), ]
aoall$Value <- runif(nrow(aoall))
aoall_shk <- ems_shock(
 var = "aoall",
 type = "custom",
 input = aoall)

}
