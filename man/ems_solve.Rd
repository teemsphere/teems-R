% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ems_solve.R
\name{ems_solve}
\alias{ems_solve}
\title{Configure and solve model}
\usage{
ems_solve(
  cmf_path,
  solution_method = c("Johansen", "Gragg", "Euler", "RK2", "RK4", "BoSha32", "DoPri54"),
  matrix_method = c("auto", "LU", "DBBD", "SBBD", "NDBBD"),
  n_subintervals = 1L,
  steps = c(2L, 4L, 8L),
  adaptive = c("no", "yes", "accuracy-only"),
  eps_tolerance = 0.1,
  n_tasks = 1L,
  laA = 300L,
  laD = 200L,
  laDi = 500L,
  inmemory = NULL,
  verbosity = NULL,
  suppress_outputs = FALSE,
  terminal_run = FALSE,
  assertions = NULL,
  range_test_initial = NULL,
  range_test_updated = NULL,
  postsim = NULL,
  complementarity = NULL,
  append_args = NULL,
  pre_probe = FALSE
)
}
\arguments{
\item{cmf_path}{Character length 1, path to the CMF file
generated by \code{\link[=ems_deploy]{ems_deploy()}}.}

\item{solution_method}{The solution method to be used, default
is the one-step \code{"Johansen"}. Choices:
\itemize{
\item \code{"Johansen"}: The Johansen method is fast however it should
only be used as a rough approximation due to handling of
nonlinear equations. See:
\href{https://www.copsmodels.com/gpmanual.htm#gpd1.2.13}{Johansen}
\item \code{"Gragg"}: Gragg's method (the smoothed modified midpoint
method; Pearson 1991). The shock is applied over a sequence
of linearized steps — an Euler first step, midpoint
"leapfrog" steps, and a terminal smoothing pass — and the
sequence is repeated for each of the three step counts in
\code{steps}. The three solutions are then combined by Richardson
extrapolation, giving rapid error decay as steps increase
plus an accuracy estimate from the agreement between levels.
Recommended whenever accuracy matters. See also:
\href{https://www.copsmodels.com/gpmanual.htm#gpd3.12.2}{GEMPACK
solution methods}
\item \code{"Euler"}: The forward Euler method, computed for each of
the three step counts in \code{steps} and combined by Richardson
extrapolation. Converges more slowly than \code{"Gragg"} (its
truncation error shrinks linearly in the step size, so the
extrapolated accuracy gains one order per solution instead of
two) but it is the most robust of the multistep methods: each
step uses only the current state, so it tolerates
near-asymptote levels and severe shocks — including exact
\verb{-100\%} shocks — where the midpoint family can become
unstable. Any strictly increasing step counts are permitted.
\item \code{"RK2"}, \code{"RK4"}: Explicit Runge-Kutta methods (the midpoint
method and the classic fourth-order method; Schiffmann 2022).
A single solution is computed with the one step count in
\code{steps} — no Richardson extrapolation — with each step built
from 2 (\code{"RK2"}) or 4 (\code{"RK4"}) staged linear solves. \code{"RK4"}
reaches a given accuracy in far fewer steps than the
extrapolating methods.
\item \code{"BoSha32"}, \code{"DoPri54"}: Embedded Runge-Kutta pairs
(Bogacki-Shampine 3(2), 4 stages; Dormand-Prince 5(4), 7
stages). Alongside the solution they compute a
component-by-component estimate of the cumulative error —
returned as the \code{error_metric} column by \code{\link[=ems_compose]{ems_compose()}}
and summarized as a face value in the solver log — and they
support adaptive step-size control via \code{adaptive} and
\code{eps_tolerance}. \code{"DoPri54"} with \code{adaptive = "yes"} is the
recommended high-accuracy choice.
}}

\item{matrix_method}{Character of length 1, matrix solution
method (default is \code{"auto"}). Choices:
\itemize{
\item \code{"auto"}: Selects the method from the model type and size
(see Details). The selection is reported at run time.
\item \code{"LU"}: Standard LU decomposition, the most robust and
potentially slowest for a large model. For use with both
static and dynamic models.
\item \code{"DBBD"}: Doubly bordered block diagonal, parallel solution
method for static models. Potentially faster than \code{"LU"}
although less robust.
\item \code{"SBBD"}: Singly bordered block diagonal, parallel solution
method for intertemporal models. Potentially faster than
\code{"LU"} although less robust.
\item \code{"NDBBD"}: Nested doubly bordered block diagonal, parallel
solution method for large intertemporal models with many
timesteps.
}}

\item{n_subintervals}{Integer length 1 (default is \code{1L}),
number of subintervals for the applied shock. More
subintervals may alleviate accuracy issues stemming from
large shock magnitudes.}

\item{steps}{Integer length 3 (default is \code{c(2L, 4L, 8L)}). A
strictly increasing vector of step counts for the three
extrapolation solutions of the \code{"Gragg"} and \code{"Euler"}
methods. A larger number of steps may improve accuracy for
some model runs. For \code{"Gragg"} all three must additionally be
even, because the error-cancellation theory behind the
extrapolation assumes even step counts (Pearson 1991,
Theorem 6.1); \code{"Euler"} has no parity requirement. The
Runge-Kutta methods compute a single solution and instead
take one step count (e.g. \code{steps = 8L}), which under
\code{adaptive} control is the initial step count only. Ignored
when \code{solution_method = "Johansen"}.}

\item{adaptive}{Character length 1, adaptive step-size control
for the embedded Runge-Kutta methods (\code{"BoSha32"},
\code{"DoPri54"}); default is \code{"no"}. Choices:
\itemize{
\item \code{"no"}: Fixed steps.
\item \code{"yes"}: After each step the worst per-component error
metric is compared against \code{eps_tolerance}; failing steps are
redone with a smaller step size and passing steps adjust the
next step size (at most halving or doubling it). A step on
which a percentage-change variable crosses \verb{-100\%} is also
retried at a reduced step size.
\item \code{"accuracy-only"}: As \code{"yes"}, but only the error metric is
acted on; check failures are ignored.
}}

\item{eps_tolerance}{Numeric length 1 (default is \code{0.1}), the
per-step error-metric bound targeted by \code{adaptive} control.
\code{0.1} suffices for most simulations; use \code{0.01} for more
accurate solutions. Values below \code{0.005} are hard to achieve
numerically. Ignored when \code{adaptive = "no"}.}

\item{n_tasks}{Integer length 1 (default is \code{1L}), number of
tasks to run in parallel. Must be \code{1L} if \code{"matrix_method"}
== "LU".}

\item{laA}{Integer length 1 (default is \code{300L}). Memory
parameter (\%) for \code{"LU"} and \code{"SBBD"}.}

\item{laD}{Integer length 1 (default is \code{200L}). Memory
parameter (\%) for \code{"DBBD"} and \code{"NDBBD"}.}

\item{laDi}{Integer length 1 (default is \code{500L}). Memory
parameter (\%) for \code{"NDBBD"} only.}

\item{inmemory}{Logical length 1 or \code{NULL} (default). When
\code{TRUE}, the solver keeps value arrays and block factors
resident in memory instead of spilling them to scratch
files; when \code{FALSE}, scratch files are used. The default
(\code{NULL}) lets the solver choose per matrix method (currently
in-memory for all methods except \code{"NDBBD"}). The solver
falls back to scratch files with a warning if the estimated
memory requirement exceeds what is available.}

\item{verbosity}{Integer length 1 (\code{0}, \code{1}, or \code{2}) or \code{NULL}
(default, equivalent to the solver default of \code{1}). Solver
log detail: \code{0} restricts output to errors, warnings, and
the accuracy summary; \code{1} adds phase progress and timings;
\code{2} adds per-rank and per-block debug detail.}

\item{suppress_outputs}{Logical length 1 (default is \code{FALSE}).
When \code{TRUE} solver outputs are not automatically converted
into structured data with \code{\link[=ems_compose]{ems_compose()}}.}

\item{terminal_run}{Logical length 1 (default is \code{FALSE}).
When \code{TRUE}, the function is exited without running the
solver. This allows the user to close any R IDE or other
programs prior to running from the terminal. When \code{TRUE}
solver outputs are not automatically converted into
structured data with \code{\link[=ems_compose]{ems_compose()}}.}

\item{assertions}{Character length 1, \code{"fatal"}, \code{"warn"} or
\code{"off"} (default \code{NULL}, solver default \code{"fatal"}). Severity of
TAB \code{Assertion} statement failures (GEMPACK manual 25.3):
\code{"warn"} reports and continues, \code{"off"} skips the checks.}

\item{range_test_initial}{Character length 1, \code{"fatal"}, \code{"warn"}
or \code{"off"} (default \code{NULL}, solver default \code{"warn"}). Severity of
declared-range violations (e.g. \verb{(ge 0)}) on initial values
(GEMPACK manual 25.4.4).}

\item{range_test_updated}{Character length 1, \code{"fatal"}, \code{"warn"}
or \code{"off"} (default \code{NULL}, solver default \code{"warn"}). As
\code{range_test_initial}, for updated values.}

\item{postsim}{Logical length 1 (default \code{NULL}, solver default
\code{TRUE}). \code{FALSE} skips the TAB's \code{PostSim} sections.}

\item{complementarity}{A \code{teems_complementarity} object built by
\code{\link[=ems_complementarity]{ems_complementarity()}} (default is \code{NULL}), run controls for
models with active \code{Complementarity} statements (GEMPACK manual
ch. 51). \code{NULL} applies the solver defaults; ignored by the
solver when the model has no active complementarity component.}

\item{append_args}{Character vector (default \code{NULL}).
Additional arguments appended to the Docker run command
(e.g., \code{c("-smllthreads 2", "-maxthreads 2")}).}

\item{pre_probe}{Logical length 1 (default \code{FALSE}). When \code{TRUE},
run the solver's structural probe first and abort — with the
defective variable and equation elements named — if the deployed
system is structurally singular, instead of failing mid-solve
with an unnamed singularity. Adds a probe run's cost (the
pre-solve pipeline plus a maximum matching: negligible below
~10^5 equations, tens of seconds around 10^6). See
\code{\link[=ems_probe]{ems_probe()}} for the full diagnosis.}
}
\value{
A tibble of model output variables and coefficients.
\code{invisible(NULL)} if \code{suppress_outputs = TRUE}. Instructions
for terminal execution if \code{terminal_run = TRUE}.
}
\description{
Calls the
\href{https://github.com/teemsphere/teems-solver}{teems-solver}
Docker image to solve the constrained optimization problem.
Runs singularity, accuracy, and error checks on the results
and structures outputs with \code{\link[=ems_compose]{ems_compose()}}.
}
\details{
\code{matrix_method = "auto"} selects \code{"SBBD"} for
intertemporal models (fastest on every benchmarked shape,
including single-task runs) and \code{"LU"} for static models,
switching to \code{"DBBD"} when \code{n_tasks >= 2} and the deployed
system is large (roughly 2 million or more equations, or 1.5
million with 100+ regions). The selection is reported at run
time; set \code{matrix_method} explicitly to override it. Runs
without deploy metadata (e.g. \code{\link[=solve_in_situ]{solve_in_situ()}}) fall back
to \code{"LU"} for static models regardless of size.

Increase \code{laA}, \code{laD}, or \code{laDi} gradually if the
solver returns "Error return from MA48B/BD because LA is
...". The relevant parameter depends on the \code{matrix_method}
in use (see above).
}
\examples{
\dontrun{
# The following examples require the teems solver to be built. 
# See https://teemsphere.github.io/ to get started.
 
# Solving a static model with Johansen:
ems_solve(cmf_path)

# Solving a dynamic model with the SBBD method:
ems_solve(cmf_path,
          solution_method = "Gragg",
          matrix_method = "SBBD",
          n_tasks = 6)
}
}
\references{
Pearson, K.R. (1991), "Solving Nonlinear Economic
Models Accurately via a Linear Representation", Impact
Project Preliminary Working Paper No. IP-55, Monash
University (revised June 2002).

Schiffmann, F. (2022), "Runge Kutta integrators for fast and
accurate solutions in GEMPACK".
}
\seealso{
\code{\link[=ems_deploy]{ems_deploy()}} for generating \code{"cmf_path"}.
\code{\link[=solve_in_situ]{solve_in_situ()}} for calling the solver on existing input
files. \code{\link[=ems_compose]{ems_compose()}} for structuring data when
\code{"suppress_outputs"} or \code{"terminal_run"} is \code{TRUE}.
}
