% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ems_options.R
\name{ems_option_get}
\alias{ems_option_get}
\title{Get default package options}
\usage{
ems_option_get(name = NULL)
}
\arguments{
\item{name}{Name of the option for which to retrieve a
value. One of:
\itemize{
\item \code{NULL} Returns all option values.
\item \code{"verbose"} Logical. If \code{FALSE}, function-specific
diagnostics are silenced.
\item \code{"tempdir"} Character. Directory used for temporary
file storage during a model run.
\item \code{"ndigits"} Integer. Exact number of digits to the
right of the decimal point written to file for numeric
type double.
\item \code{"accuracy_threshold"} Numeric. Threshold
(converted to a percentage) against which 4-digit
precision is compared.
\item \code{"check_shock_status"} Logical. If \code{FALSE}, no
check on shock element endogenous/exogenous status is
conducted.
\item \code{"timestep_header"} Character. Coefficient
containing a numeric vector of timestep intervals.
\item \code{"n_timestep_header"} Character. Coefficient
containing a numeric vector length one with sum of
timestep intervals.
\item \code{"full_exclude"} Character vector. Headers to
fully exclude from all aspects of the model run.
\item \code{"docker_tag"} Character. Docker tag specifying
which Docker image to use. \code{"latest"} when unset; note
the solve command auto-selects a host-matched variant
tag when the option is unset (see \code{\link[=ems_option_set]{ems_option_set()}}).
}}
}
\value{
If \code{name} is \code{NULL}, a named list of all current
option values. Otherwise, the value of the requested option.
}
\description{
\code{ems_option_get()} returns default package
options. If \code{name} is \code{NULL} (the default), all option
values are returned as a list.
}
\examples{
# Retrieve all options values
ems_option_get()

# Retrieve option value for `ndigits`
ems_option_get("ndigits")
}
\seealso{
\code{\link[=ems_option_set]{ems_option_set()}} for setting package options.
\code{\link[=ems_option_reset]{ems_option_reset()}} for resetting package options.
}
