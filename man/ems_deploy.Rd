% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ems_deploy.R
\name{ems_deploy}
\alias{ems_deploy}
\title{Validate and write all model inputs}
\usage{
ems_deploy(
  .data,
  model,
  shock = NULL,
  swap_in = NULL,
  swap_out = NULL,
  shock_file = NULL,
  write_coefficients = FALSE
)
}
\arguments{
\item{.data}{A list of data.tables generated by
\code{\link[=ems_data]{ems_data()}}.}

\item{model}{A tibble generated by \code{\link[=ems_model]{ems_model()}}.}

\item{shock}{A \code{shock} object or list of \code{shock} objects
produced by \code{\link[=ems_uniform_shock]{ems_uniform_shock()}}, \code{\link[=ems_custom_shock]{ems_custom_shock()}},
or \code{\link[=ems_scenario_shock]{ems_scenario_shock()}}. When \code{NULL} (default), no shock
is applied and input data is returned without change.}

\item{swap_in}{A character vector of variable length, list
generated by \code{\link[=ems_swap]{ems_swap()}}, or combination of the above
provided as a list (default \code{NULL}). Variables to be made
exogenous.}

\item{swap_out}{A character vector of variable length, list
generated by \code{\link[=ems_swap]{ems_swap()}}, or combination of the above
provided as a list (default \code{NULL}). Variables to be made
endogenous.}

\item{shock_file}{A character vector length 1 (default
\code{NULL}). Path to a file with .shf extension representing a
fully prepared shock file. No checks or modifications are
carried out on this file.}

\item{write_coefficients}{Logical of length 1 (default \code{FALSE}).
Coefficient values return to R through the solver's binary
coefficient dump, read selectively by \code{\link[=ems_compose]{ems_compose()}}. Set
\code{TRUE} to also emit the GEMPACK-style per-coefficient CSV files
(\verb{out/coefficients/}, \verb{out/postsim/}) via auto-generated
\code{File}/\code{Write} statements, e.g. for inspection outside R.}
}
\value{
Path to the CMF file, to be passed to \code{\link[=ems_solve]{ems_solve()}}.
}
\description{
Validates all data and model inputs, processes
shocks and closure swaps, and writes the input files
required by \code{\link[=ems_solve]{ems_solve()}}.
}
\examples{
\dontrun{
# The following examples require input data. See
# https://teemsphere.github.io/ to get started.

# Uniform shock applied to full variable with value 1
shock <- ems_uniform_shock("qfd", value = 1)

# Full variable swap, qfd for tfd
cmf_path <- ems_deploy(
  .data = dat,
  model = model,
  shock = shock,
  swap_in = "qfd",
  swap_out = "tfd"
)

# Partial variable swap (element "row" of set REG with index r)
yp_row <- ems_swap("yp", REGr = "row")
dppriv_row <- ems_swap("dppriv", REGr = "row")

# Swap part of yp for part of dppriv; qfd for tfd
cmf_path <- ems_deploy(
 .data = dat,
 model = model,
 shock = shock,
 swap_in = list(yp_row, "qfd"),
 swap_out = list(dppriv_row, "tfd")
)
}
}
\seealso{
\code{\link[=ems_data]{ems_data()}} for loading and preparing data inputs.
\code{\link[=ems_model]{ems_model()}} for parsing and modifying model and closure
files. \code{\link[=ems_uniform_shock]{ems_uniform_shock()}}, \code{\link[=ems_custom_shock]{ems_custom_shock()}}, and
\code{\link[=ems_scenario_shock]{ems_scenario_shock()}} for preparing shocks.
\code{\link[=ems_swap]{ems_swap()}} for preparing closure swaps. \code{\link[=ems_solve]{ems_solve()}}
for utilizing the output of this function.
}
